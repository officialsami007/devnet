node_modules
.next
out
.env
.env.local
.DS_Store
npm-debug.log*

# Build / tooling artifacts
tsconfig.tsbuildinfo

# Local session / working artifacts (not part of the project)
.claude/
.playwright-mcp/
# Root-level screenshots from the working session (keep public/ assets tracked)
/*.png
